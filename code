value=1000
number=900
print("Prime numbers between", value, "and", number, "are:"

for number in range(value, number + 1):
  
   if number > 1:
       for i in range(2, number):
           if (number % i) == 0:
               break
       else:
           print(number)
